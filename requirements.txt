
# Core app
streamlit>=1.33
pandas>=2.0

# Excel support
openpyxl>=3.1    # for .xlsx/.xlsm reading
xlrd==1.2.0      # legacy .xls support (the last xlrd version that can read .xls)

# Optional: if you run into Excel date/time quirks, pandas depends on numpy automatically
# numpy>=1.24
